import React, { useState, useEffect, useContext } from "react";

const PageBorrow = () => {

  return (
    <div className="w-full h-screen items-center justify-center z-100 bg-customRed">
      <div className="text-ellSecondary text-lg font-inter font-extrabold">Toolhub</div>
    </div>
  );
};

export default PageBorrow;
